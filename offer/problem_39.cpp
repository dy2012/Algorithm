#include <algorithm>
#include <vector>
#include <iostream>

using namespace std;

int findLargeMid(const vector<int>& nums) {
    return 0;
}

int main(int argc, char* argv[]) {
    vector<int> nums = {1, 2, 3, 2, 2, 2, 5, 4, 2};
    cout << nums.size() << endl;
    cout << nums.capacity() << endl;
    return 0;
}
